/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow Next.js to serve pages while Express handles the API routes
  // when running as a custom server (src/server.js)
};

module.exports = nextConfig;
